## Run using the Go backend
# Run

1. Start the server from the project directory:

   ```bash
   go run .
   ```

2. Open [http://localhost:8080](http://localhost:8080) in your browser.
   Enter a letter and click **Count cities** to send a request to
   `/api/cities/count?letter=<letter>` on the running server.